id: reearth-text-plugin
name: Text Plug-in
version: 1.0.0
extensions:
  - id: text
    type: widget
    name: Text Widget
    schema:
      groups:
        - id: default
          fields:
            - id: text
              type: string
              title: Text
